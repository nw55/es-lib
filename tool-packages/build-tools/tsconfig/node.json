{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "./base.json",
    "compilerOptions": {
        // keep in sync with node-lib.json (except for "module")
        "module": "CommonJS",
        "types": [
            "node"
        ]
    }
}
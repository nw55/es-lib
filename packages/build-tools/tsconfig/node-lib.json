{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "./lib.json",
    "compilerOptions": {
        // keep in sync with node.json (except for "module")
        "types": [
            "node"
        ]
    }
}